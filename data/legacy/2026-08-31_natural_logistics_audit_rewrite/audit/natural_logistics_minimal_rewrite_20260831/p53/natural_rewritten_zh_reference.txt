初始时，我在若干城市和地点中拥有一批包裹和运输工具。具体而言，对于从 1 到 10 的每个索引 i，三个包裹标签均按依次写出 obj、i，再写 1、2 或 3 的方式构成；卡车标签从 tru1 到 tru10，城市标签从 cit1 到 cit10，而地点包括位置区域和机场，分别从 pos1 到 pos10 以及从 apt1 到 apt10。此外，还有三架名为 apn1、apn2 和 apn3 的飞机，它们依次位于机场 apt8、apt3 和 apt4。每座城市都包含一个位置区域和一个机场：对于从 1 到 10 的每个索引 i，pos 后接 i 和 apt 后接 i 的地点都位于 cit 后接同一 i 的城市中。每辆卡车起初都在各自城市中的相应位置，并与一组包裹在一起。对于从 1 到 10 的每个索引 i，tru 后接 i 的卡车以及按依次写出 obj、i，再写 1、2 或 3 所构成的三个包裹标签，都位于 pos 后接同一 i 的地点。我的目标是将这些包裹运送到指定的最终地点。我希望 obj83 位于 pos4、obj63 位于 pos7、obj101 也位于 pos7，并且 obj92 位于 pos3。此外，obj52 应到达 pos10、obj43 到达 pos6，而 obj82 到达 pos9。其他具体目标包括将 obj72 运到 pos4、obj32 运到 apt8，以及 obj33 运到 apt5。还需要移动包裹，使 obj81 最终位于 pos4、obj13 位于 apt2，而 obj42 位于 pos9。此外，obj21 应位于 apt5、obj11 位于 apt1，而 obj73 位于 pos1。obj62、obj41、obj12、obj93、obj22、obj102、obj53、obj61、obj51、obj71、obj23 和 obj103 的目标地点依次是 apt3、pos7、apt6、pos5、pos9、apt4、pos2、apt2、apt2、pos7、apt5 和 apt9。挑战是高效地将所有包裹重新安置到各自的目标地点。
