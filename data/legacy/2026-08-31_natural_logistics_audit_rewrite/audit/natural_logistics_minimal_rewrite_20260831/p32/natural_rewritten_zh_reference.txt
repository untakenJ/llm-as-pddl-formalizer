在 Logistics 游戏中，对象和初始条件如下。包裹为 obj11、obj12、obj13、obj21、obj22、obj23、obj31、obj32、obj33、obj41、obj42、obj43、obj51、obj52、obj53、obj61、obj62、obj63。卡车为 tru1、tru2、tru3、tru4、tru5、tru6。飞机为 apn1、apn2。城市为 cit1、cit2、cit3、cit4、cit5、cit6。地点为 pos1、apt1、pos2、apt2、pos3、apt3、pos4、apt4、pos5、apt5、pos6、apt6。机场地点为 apt1、apt2、apt3、apt4、apt5、apt6。初始时，飞机位置如下：apn1 在 apt2；apn2 在 apt3。初始时，卡车位置如下：tru1 在 pos1；tru2 在 pos2；tru3 在 pos3；tru4 在 pos4；tru5 在 pos5；tru6 在 pos6。初始时，包裹位置如下：obj11 在 pos1；obj12 在 pos1；obj13 在 pos1；obj21 在 pos2；obj22 在 pos2；obj23 在 pos2；obj31 在 pos3；obj32 在 pos3；obj33 在 pos3；obj41 在 pos4；obj42 在 pos4；obj43 在 pos4；obj51 在 pos5；obj52 在 pos5；obj53 在 pos5；obj61 在 pos6；obj62 在 pos6；obj63 在 pos6。地点与城市的关系为：pos1 在 cit1；apt1 在 cit1；pos2 在 cit2；apt2 在 cit2；pos3 在 cit3；apt3 在 cit3；pos4 在 cit4；apt4 在 cit4；pos5 在 cit5；apt5 在 cit5；pos6 在 cit6；apt6 在 cit6。目标是让包裹到达以下目的地：obj63 必须在 apt6；obj11 必须在 apt1；obj41 必须在 apt2；obj21 必须在 pos2；obj12 必须在 pos1；obj51 必须在 pos2；obj62 必须在 pos1；obj61 必须在 apt2；obj53 必须在 pos3；obj42 必须在 apt3；obj22 必须在 pos3；obj43 必须在 pos2；obj23 必须在 apt5；obj52 必须在 pos5；obj13 必须在 apt6；obj33 必须在 pos2；obj31 必须在 pos3；obj32 必须在 pos3。
