物流游戏的初始设置涉及若干包裹、卡车、城市、地点、机场和飞机。存在以下包裹：obj11、obj12、obj13、obj21、obj22、obj23、obj31、obj32、obj33、obj41、obj42、obj43、obj51、obj52、obj53、obj61、obj62、obj63、obj71、obj72、obj73、obj81、obj82、obj83、obj91、obj92、obj93、obj101、obj102、obj103、obj111、obj112、obj113、obj121、obj122 和 obj123。有十二辆卡车：tru1 至 tru12。游戏中有十二座城市：cit1 至 cit12，每座城市都包含一个“pos”地点和一个机场（“apt”）。位置 pos1 至 pos12 和机场 apt1 至 apt12 映射到其各自对应的城市。特别地，apt1 至 apt12 也分别作为所在城市的机场。三架飞机 apn1、apn2 和 apn3 分别位于 apt5、apt2 和 apt1。对于从 1 到 12 的每个整数 X，卡车 truX 以及包裹 objX1、objX2 和 objX3 初始位于 posX；每个标识符中的 X 都替换为同一个十进制数字表示（例如 X = 10 时得到 tru10、obj101、obj102、obj103 和 pos10）。每个位置地点 posX 位于其对应城市 citX 中，机场 aptX 也位于同一城市 citX 中。

物流游戏的目标是把包裹置于指定地点：将 obj82 放在 apt1，将 obj103 移到 pos1，将 obj101 移到 apt5，将 obj31 送到 apt1，将 obj11 放在 apt4，使 obj43 位于 pos10，将 obj42 移到 pos3，将 obj12 移到 pos11，将 obj112 放在 apt4，将 obj83 放在 apt8，将 obj22 送到 pos7，使 obj53 保持在 pos5，将 obj41 移到 apt12，将 obj123 移到 apt9，将 obj93 放在 pos6，将 obj32 移到 pos1，将 obj113 送到 apt8，将 obj23 放在 apt1，将 obj13 移到 pos9，将 obj52 移到 pos10，将 obj33 送到 pos4，将 obj63 放在 apt3，将 obj121 移到 pos6，将 obj91 移到 apt5，将 obj51 放在 apt1，将 obj21 送到 pos3，将 obj81 移到 apt11，将 obj111 放在 apt7，将 obj62 移到 apt2，将 obj122 移到 apt2，将 obj92 放在 apt6，将 obj71 移到 apt1，将 obj73 放在 pos4，最后将 obj61 移到 pos10。
