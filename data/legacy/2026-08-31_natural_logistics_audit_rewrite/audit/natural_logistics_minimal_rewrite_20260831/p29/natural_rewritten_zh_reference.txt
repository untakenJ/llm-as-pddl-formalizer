在 Logistics 游戏中，初始设置如下：obj11、obj12、obj13、obj21、obj22、obj23、obj31、obj32、obj33、obj41、obj42、obj43、obj51、obj52、obj53、obj61、obj62 和 obj63 被指定为包裹。有若干辆标记为 tru1 至 tru6 的卡车，它们在标记为 cit1 至 cit6 的各城市中运行。每座城市包含两个地点：一个位置（pos1 至 pos6）和一个机场（apt1 至 apt6）。机场地点分别是 cit1 的 apt1、cit2 的 apt2、cit3 的 apt3、cit4 的 apt4、cit5 的 apt5 和 cit6 的 apt6。此外，有两架飞机 apn1 和 apn2，分别停在机场 apt3 和 apt2。初始时，所有卡车都停在各自的位置：tru1 在 pos1，与包裹 obj11、obj12 和 obj13 同处；tru2 在 pos2，与包裹 obj21、obj22 和 obj23 同处；tru3 在 pos3，与包裹 obj31、obj32 和 obj33 同处；tru4 在 pos4，与包裹 obj41、obj42 和 obj43 同处；tru5 在 pos5，与包裹 obj51、obj52 和 obj53 同处；tru6 在 pos6，与包裹 obj61、obj62 和 obj63 同处。每个位置都对应一座城市：pos1 在 cit1，pos2 在 cit2，pos3 在 cit3，pos4 在 cit4，pos5 在 cit5，pos6 在 cit6。目标是将指定包裹移到指定地点：将 obj13 移到 pos5，obj52 移到 pos6，obj12 移到 apt2，obj53 移到 apt3，obj63 移到 pos6，obj21 移到 pos5，obj33 移到 pos1，obj51 移到 pos3，obj31 移到 apt3，obj42 移到 apt3，obj61 移到 pos6，obj11 移到 apt3，obj32 移到 pos1，obj43 移到 pos6，obj62 移到 apt6，obj41 移到 pos1，并将 obj23 移到 pos4。
