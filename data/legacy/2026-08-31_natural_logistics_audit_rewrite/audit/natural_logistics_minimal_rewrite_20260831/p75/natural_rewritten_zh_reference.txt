在物流游戏中，初始设置包含各种包裹、卡车、飞机、城市和地点。初始时，对于从 1 到 13 的每个整数 i，恰有三个包裹，其名称由 obj、i 以及 1、2、3 中的每一个依次拼接而成；有一辆由 tru 与 i 拼接而成的卡车；一座由 cit 与 i 拼接而成的城市；以及两个分别由 pos 与 i、apt 与 i 拼接而成的地点，其中后者同时是机场。此外，有四架飞机 apn1 至 apn4，初始时停在指定机场。具体而言，apn1 位于 apt12，apn2 位于 apt7，apn3 位于 apt1，apn4 位于 apt2。对于从 1 到 13 的每个整数 i，由 pos 与 i 拼接而成的地点和由 apt 与同一个 i 拼接而成的机场都位于由 cit 与 i 拼接而成的城市中。对于从 1 到 13 的每个整数 i，由 tru 与 i 拼接而成的卡车，以及由 obj、i 和 1、2、3 中的每一个依次拼接而成的三个包裹，最初都位于由 pos 与同一个 i 拼接而成的地点。游戏目标是把这些包裹移动到指定地点：将 obj123 移至 apt6、将 obj23 移至 pos6、将 obj121 移至 pos2、将 obj101 移至 apt8、将 obj113 移至 pos6、将 obj21 移至 apt8、将 obj132 移至 apt5、将 obj13 移至 pos1、将 obj62 移至 pos12、将 obj63 移至 apt6、将 obj133 移至 pos1、将 obj33 移至 apt7、将 obj22 移至 apt4、将 obj92 移至 pos6、将 obj111 移至 pos2、将 obj42 移至 pos9、将 obj52 移至 pos11、将 obj53 移至 apt9、将 obj122 移至 apt11、将 obj82 移至 pos8、将 obj81 移至 apt6、将 obj93 移至 pos13、将 obj73 移至 pos8、将 obj83 移至 pos4、将 obj72 移至 apt11、将 obj31 移至 apt7、将 obj91 移至 pos9、将 obj61 移至 apt7、将 obj43 移至 pos6、将 obj51 移至 apt10、将 obj41 移至 apt4、将 obj103 移至 apt12、将 obj32 移至 pos5、将 obj112 移至 pos2、将 obj131 移至 pos11、将 obj12 移至 apt1、将 obj11 移至 pos4、将 obj71 移至 pos10。

